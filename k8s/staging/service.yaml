apiVersion: v1
kind: Service
metadata:
  name: staging-librc2d-frontend-service
  namespace: staging-librc2d-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: staging-librc2d-frontend
